#include <stdio.h>
#include <math.h>

int main ()
{

    float arrx[4] = {1.0,2.0,3.0,4.0}, arry[4]={6.0,11.0,18.0,27.0}, arrc[3][3];
    int n = 4, mp = 2, m, i, j, k;

    if (n<= mp) printf("Regression not possible.");
    else {
        m = mp+1;
        for (j= 1; j<=m; j++)
        {
            for (k=1;k<=m; k++)
            {
                for (i=0; k<=m; k++)
                {
                    arr[j][k]=
                }
            }
        }
    }

    return 0;
}
